# comment
from-conf=value-conf
priority-conf=value-conf
# quoted values should be unquoted before setting the flag
quoted-conf="value-conf"
# bool values don't need to be specified, their presence is enough
quoted-bool="true"
naked-bool
normal-bool=true
normal-false-bool=false

