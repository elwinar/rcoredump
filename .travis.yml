git:
  depth: 3
  quiet: true
  submodules: false
language: go
go:
  - 1.13.x
  - 1.14.x
cache:
  directories:
    - $HOME/.cache/go-build
    - $GOPATH/pkg/mod
before_install:
  - nvm install node
install:
  - make install
script:
  - make web
  - make rcoredump
  - make rcoredumpd
